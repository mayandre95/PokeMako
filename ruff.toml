target-version = "py311"

[lint]
select = ["F", "I", "UP", "B", "SIM", "BLE"]
ignore = [
    "B008",   # FastAPI Depends() dans les arguments est idiomatique
    "BLE001", # Exception large dans la boucle ETL est intentionnelle
]
